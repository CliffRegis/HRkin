<%= form_for(current_user.active_collaborations.find_by(supported_id: @user.id), html: { method: :delete }) do |f| %>
<div><%= hidden_field_tag :supported_id, @user.id %></div>
  <%= f.submit "Un-Support", class: "btn btn-danger" %>
<% end %>


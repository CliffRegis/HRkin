<div>
  ******************
show my friends wiki


****************


</div>



<div class="page-header">
  <center><strong><h1> Wiki </h1></strong></center>
</div>

 
<% @pages.each do |page| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to page.title, [page.user, page] %>
      </h4>
      <h5 class="media-heading">
        <%= truncate (page.content) %> <br>
       <small>Project by:</small> <%= link_to page.user.handle, [page.user, page] %>&nbsp<%= image_tag(page.user.avatar.small.url, class: "img-circle") %>
      </h5>
       
    </div>
      <%#= will_paginate @pages %>
  </div>
  <hr>
<% end %>

<br>
<br>
<%= link_to 'New wiki', new_user_page_path, class: 'btn btn-success' 'alert alert-success'  %>  


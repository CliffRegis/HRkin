

<%= form_for(current_user.active_collaborations.build(supported_id: @user.id)) do |f| %>
  <div><%= hidden_field_tag :supported_id, @user.id %></div>
  <%= f.submit "Add support", class: "btn btn-primary" %>
<% end %>


